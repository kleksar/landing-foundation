# Контекст проекта

## Проект и задача

- Режим: `foundation` — разработка общего шаблона, клиентского заказа здесь нет.
- Источник задачи: согласованный план в [дорожной карте](docs/roadmap.md).
- Цель: один независимо оформленный статический сайт на копию репозитория;
  агент выполняет техническую работу и сохраняет контекст между сессиями.

## Подтверждённые решения

- Bun / React / Vite / TypeScript; один сайт, без обязательного backend.
- Работа небольшими этапами, TDD для изменений поведения, независимое ревью.
- Этап 2.2 принят: 57 тестов, чистая установка, dev и preview проверены.
- Демонстрационные тексты, оранжевый акцент и `example.com` относятся только к starter.
  Это не требования или домен будущего клиента.

## Материалы и ограничения

- Требования демонстрации: [SITE_SPEC.md](SITE_SPEC.md); её оформление: [DESIGN.md](DESIGN.md).
- Клиентские загрузки в этой сессии не включаются в общий шаблон и примеры.
- Репозиторий: https://github.com/kleksar/landing-foundation (public). Чтение кода через
  GitHub-подключение и локальный clone проверены. В новой сессии проверить актуальный
  checkout; сам URL или архив в старой переписке не доказывает текущий доступ.

## Открытые вопросы

- Блокирующих вопросов для разработки основы нет.
- Хостинг, публикация и проверка подключения навыков в разных средах — отдельные задачи.

## Текущее состояние и следующий шаг

- Завершены 3.1–3.4 (вход, содержание, дизайн, средства) и 4.1 (проба реализации).
- Перенос проекта в корень применён пользователем и отправлен по SSH: GitHub `main`,
  коммит `c9f417c`. Приложение, зависимости и навыки сохранены.
- [Первый основной CI](https://github.com/kleksar/landing-foundation/actions/runs/34063589819)
  на этом коммите — **success**: lint, typecheck, build, 34 unit, 10 repository,
  7 build contracts и 8 full Chromium E2E — **59 тестов**; audit не нашёл уязвимостей.
  Использован Chrome for Testing 151.0.7922.34. Это последний подтверждённый полный verify.
- Для продолжения 4.2 добавлен отдельный `compatibility` workflow: Chrome Stable,
  Firefox и WebKit, общие 24 E2E, фактические версии в логе. Он запускается вручную или
  при push изменений своей конфигурации/тестов; подробности — [testing](docs/testing.md).
- Новый CI-контракт прошёл red → green. Удалённый запуск нового workflow ещё не выполнен:
  приёмка 4.2 остаётся открытой до успешных 24 проверок совместимости.
- Текущий локальный `verify`: lint, typecheck, build и 52 теста (34 / 11 / 7) прошли;
  итог exit 1 из-за прежнего запрета `socket()` в 7 Chromium E2E, 1 HTTP-тест прошёл.
  CLI подтвердил выбор 24 compatibility E2E. Независимое ревью — без замечаний.
- Следующий шаг — применить новый патч, push по SSH, проверить оба Actions run на новом
  коммите и сохранить реальные результаты. Не повторять уже применённый патч переноса.
- Запись через GitHub-подключение ранее отклонена: `403 Resource not accessible by integration`.
  Поэтому изменения передаются Git-патчем для локального push. Публичное чтение доступно.
- Локальные блокеры браузеров и исторические результаты сохранены в
  [браузерной приёмке](docs/evaluations/browsers.md); не повторять установки в той же среде.
- После 4.2 — ограниченная сравнительная проба навыков. Хостинг — этап 5.
  Автоподключение навыков в новой сессии не проверено: пробы читают AGENTS явно.
- Результаты и подробная история: [дорожная карта](docs/roadmap.md).

При начале нового клиентского сайта в отдельной копии агент заменяет эту запись контекстом
клиента и режимом `client`; не переносит факты, оформление или разрешения демонстрации.
